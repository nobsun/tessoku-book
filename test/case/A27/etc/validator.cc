#include "testlib.h"
#include <iostream>
#include <set>
using namespace std;

int main()
{
  registerValidation();
  int X = inf.readInt(1, 1000000000);
  inf.readSpace();
  int Y = inf.readInt(1, 1000000000);
  inf.readEoln();
  inf.readEof();
  return 0;
}
